#!/bin/bash

# Get (variable, new_value)
change_pair=$(edit_onx_ip.py "$@")

# Change bash variables file
change_bash_variable.py $change_pair

# Change calling environment
source change_env_var $change_pair

# refresh sublime text w/ new ONX IP
refresh_subl
